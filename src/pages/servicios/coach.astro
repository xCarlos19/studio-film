---
import ButtonBackgroundShine from "../../components/ButtonBackgroundShine";
import LayoutSecondary from "../../layouts/LayoutSecondary.astro";

const serv = [{
    title: "BÁSICO",
    price: 50,
    atributtes : [
        "3h de duración",
        "Técnicas de interpretación",
        "Control emocional",
        "Construcción de personajes",
        "Mejora en la proyección vocal",
        "Trabajo frente a la cámara",
    ],
    borderColor: "border-basic",
    buttonColor: "bg-main"

  },
  {
    title: "INTERMEDIO",
    price: 100,
    atributtes : [
        "8h de duración (divisible)",
        "Técnicas de interpretación",
        "Control emocional",
        "Construcción de personajes",
        "Mejora en la proyección vocal",
        "Trabajo frente a la cámara",
    ],
    borderColor: "border-inter",
    buttonColor: "bg-main"
  },
  {
    title: "PROFESIONAL",
    price: 140,
    atributtes : [
        "12h de duración (divisible)",
        "Técnicas de interpretación",
        "Control emocional",
        "Construcción de personajes",
        "Mejora en la proyección vocal",
        "Trabajo frente a la cámara",
    ],
    borderColor: "border-main",
    buttonColor: "bg-main"
  }

  
];

interface Props {
    title: string;
    page: string;
}
---

<LayoutSecondary title="Coach actoral | STUDIO FILMS 23" page="servicios">
    <section class="text-gray-600 body-font overflow-hidden w-full">
        <div class="container px-5 py-2 mx-auto">
          <div class="flex flex-col text-center w-full mb-16">
            <h1 class="text-3xl sm:text-6xl font-medium title-font mb-2 p-5 text-gray-900">COACH ACTORAL</h1>
            <p class="lg:w-4/5 mx-auto leading-relaxed text-sm sm:text-base text-gray-900 p-2">Un coach actoral es tu mejor guía para desarrollar y perfeccionar tu talento, preparando tu camino antes de que los directores de casting te vean en acción.</p>
            <div class="flex mx-auto border-main rounded overflow-hidden mt-6">
              <a class="py-2 px-4 cursor-pointer" href="/servicios/book">BOOK ACTORAL</a>
              <a class="py-2 px-4 border-l-main cursor-pointer" href="/servicios/videobook">VIDEOBOOK ACTORAL</a>
              <a class="py-2 px-4 bg-main text-white cursor-default">COACH ACTORAL</a>
            </div>
          </div>
          <div class="flex flex-wrap justify-center mb-8 gap-x-10 w-[80%] md:w-full m-auto">
            
            {
              serv.map((servicio) => (
              <div class="p-4 xl:w-1/4 md:w-1/2 w-full">
              <div class:list={["h-full p-6 rounded-lg border-2  flex flex-col relative overflow-hidden",servicio.borderColor]}>
                <h1 class="text-2xl sm:text-3xl l-spacing tracking-widest title-font mb-4 font-bold text-black">{servicio.title}</h1>
                
                {servicio.atributtes.map((atributte) => (
                  <p class:list={["flex items-center text-black font-text mb-2",
                  atributte === servicio.atributtes[servicio.atributtes.length - 1] && "mb-6"]
                  }>
                  <span class="w-4 h-4 mr-2 inline-flex items-center justify-center bg-black text-white rounded-full flex-shrink-0">
                    <svg fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" class="w-3 h-3" viewBox="0 0 24 24">
                      <path d="M20 6L9 17l-5-5"></path>
                    </svg>
                  </span>{atributte}
                </p>))
                  
                }
                <span class:list={["bg-red-600 text-white px-3 py-1 tracking-widest text-xs absolute right-0 top-0 rounded-bl", 
                  servicio !== serv[serv.length - 1] && "hidden"
                ]}>POPULAR</span>

                {
                  servicio === serv[serv.length - 1] ? 
                  <ButtonBackgroundShine text="Reservar" path="/contacto" />
                  :
<a class:list={["flex items-center mt-auto text-white  border-0 py-2 px-4 w-full focus:outline-none hover:scale-105 duration-150 rounded cursor-pointer", servicio.buttonColor ]} href="/#contacto">Reservar
                  <svg fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-4 h-4 ml-auto" viewBox="0 0 24 24">
                    <path d="M5 12h14M12 5l7 7-7 7"></path>
                  </svg>
                </a>
                }
                
              </div>
            </div>
              ))
            }
            
          </div>
        </div>
      </section>
</LayoutSecondary>

<style>
  .bg-main {
    background:  #500606 ;
  } 
  .bg-inter {
    background:  #8B5050 ;
  }
  .bg-basic {
    background:  #AC7474 ;
  }
  .border-inter {
    border: 2px solid #8B5050;
  }
  .border-basic {
    border: 2px solid #AC7474;
  }
  .border-main {
    border: 2px solid #500606;
  }
  .border-l-main{
    border-left: 2px solid #500606;
  }
  .l-spacing {
    letter-spacing: 0.2em;
  }
</style>